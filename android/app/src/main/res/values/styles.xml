<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- 常にダークなアプリなので、WebView が描くまでの地色も黒にする -->
        <item name="android:windowBackground">@color/engBlack</item>
        <!-- **ステータスバーとナビゲーションバーのアイコンを明るいほうに固定する。**
             親テーマが DayNight なので、端末がライトだと「明るい地の上に置く前提」で
             アイコンが黒く描かれ、**黒いアプリの上で完全に見えなくなる**
             (時計も電池も消える。エミュレータの実測で mLastAppearance=LIGHT_STATUS_BARS)。
             このアプリは端末の設定によらず常にダークなので、常に明るいアイコンでよい。
             Web 側で踏んだ「黒地に黒」と同じ罠 (docs 5章「常にダークにする」) -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">
            false
        </item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>